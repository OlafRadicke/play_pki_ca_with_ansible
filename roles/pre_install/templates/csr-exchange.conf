server {
    listen              {{ pki_csr_exchange_port }};
    server_name         foo.dum.my;
    root                {{ pki_httpd_dir }};

    location / {
                        root                 {{ pki_httpd_dir }};
                        autoindex            on;
                        autoindex_exact_size off;
                        autoindex_format     html;
                        autoindex_localtime  on;
    }
    location {{ pki_httpd_dir }}/ {
                        root                 {{ pki_httpd_dir }};
                        autoindex            on;
                        autoindex_exact_size off;
                        autoindex_format     html;
                        autoindex_localtime  on;
    }

}
